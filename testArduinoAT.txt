#include <SoftwareSerial.h>

SoftwareSerial ESP(3, 2); // ±µ¦¬¸}, ¶Ç°e¸}

void setup() {
  Serial.begin(115200);
  ESP.begin(115200);
  Serial.println("Serial is ready!");
  Serial.println("");
}
 
void loop() {
  if (Serial.available()) {
    ESP.write(Serial.read());
  }
 
  if (ESP.available()) {
    Serial.write(ESP.read());
  }
}